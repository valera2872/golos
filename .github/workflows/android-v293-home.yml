name: Build Somnori 0.29.3 home workspace

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - name: Legacy 0.29.3 workflow retained for reproducibility
        run: echo "Somnori 0.29.3 is superseded by 0.29.4 home cards fix. Use android-v294-home-cards.yml."
