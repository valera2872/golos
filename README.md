# Тихий дневник

Android-прототип голосового дневника, который в ночном режиме слушает пользовательскую кодовую фразу, сохраняет аудио и локально расшифровывает запись.

## Сборка

GitHub Actions автоматически собирает debug APK при изменениях в ветке `main`.
